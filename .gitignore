.DS_Store
*.xcuserstate
xcuserdata/
DerivedData/
*.xcworkspace/xcuserdata/
.build/
specs/
.claude/

# Security audit notes (local only)
SECURITY_AUDIT.md

# Local artifacts
validate-summary.md
*.af
IMG_*.JPG

# Demo / scratch material — local only
demo/

# Signing/export config — per-developer (carries the Team ID). Copy the
# matching *.plist.template, fill in your Team ID, and keep the real file local.
/ExportOptions-iOS.plist
/ExportOptions-macOS-direct.plist

# Credentials & signing secrets — NEVER commit, even if they land locally
*.p8
*.p12
*.cer
*.mobileprovision
*.provisionprofile
*.certSigningRequest
AuthKey_*
*-credentials
*.credentials
*credentials.json
*credentials.plist
*.env
.env
*.secret
