.DS_Store
*.xcuserstate
xcuserdata/
DerivedData/
*.xcworkspace/xcuserdata/
.build/
specs/
.claude/

# Security audit notes (local only)
SECURITY_AUDIT.md

# Local artifacts
validate-summary.md
*.af
IMG_*.JPG

# Credentials & signing secrets — NEVER commit, even if they land locally
*.p8
*.p12
*.cer
*.mobileprovision
*.provisionprofile
*.certSigningRequest
AuthKey_*
*-credentials
*.credentials
*credentials.json
*credentials.plist
*.env
.env
*.secret
